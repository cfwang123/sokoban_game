# xiyuyanapp1 — Sokoban

> [中文版](readme.zh.md)

** **： C（ ）。** **（`main.c`）， Python.

## Run

```bash
cd xiyuyanapp1
gcc -std=c11 -O2 main.c -o sokoban
./sokoban
./sokoban 3
```

Controls: WASD move, z undo, r reset, q quit.
