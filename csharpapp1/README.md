# csharpapp1 — C# 推箱子（教学）

.NET 控制台版。需要 [.NET 8 SDK](https://dotnet.microsoft.com/)。

```bash
cd csharpapp1
dotnet run
```

键位：WASD 移动，z 撤销，r 重置，q 退出。

对照：`vs2026ext1/`（Visual Studio 扩展）、`unityapp1/`（Unity C#）。
