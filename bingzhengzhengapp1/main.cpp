// bingzhengzhengapp1 — 丙正正（中文 C++ 宏）完整可玩推箱子
// 编译: g++ -std=c++17 -O2 main.cpp -o sokoban
// 运行: ./sokoban [关卡号]
#include "丙正正.hpp"
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <cctype>

#define 关卡数 12
#define 最大宽 32
#define 最大高 32
#define 最大历史 8192
#define 最大行长 256

静态 常量 字符 *关卡_0_行[] = {
    "#######",
    "#. . .#",
    "# $$$ #",
    "#.$@$.#",
    "# $$$ #",
    "#. . .#",
    "#######",
};
静态 常量 字符 *关卡_1_行[] = {
    "###",
    "#@#",
    "#$#",
    "#.#",
    "###",
};
静态 常量 字符 *关卡_2_行[] = {
    "#####",
    "#.$@#",
    "#####",
};
静态 常量 字符 *关卡_3_行[] = {
    "###",
    "#.###",
    "#*$ #",
    "#  @#",
    "#####",
};
静态 常量 字符 *关卡_4_行[] = {
    "  #####",
    "###   ##",
    "#.* #@ #",
    "##$### #",
    " #     #",
    " #######",
};
静态 常量 字符 *关卡_5_行[] = {
    "    #####",
    "  ###   #",
    "###.* # #",
    "#@$.$#  #",
    "##     ##",
    " #######",
};
静态 常量 字符 *关卡_6_行[] = {
    "#########",
    "#       #",
    "# *..## #",
    "##$#$#  #",
    " # @   ##",
    " #######",
};
静态 常量 字符 *关卡_7_行[] = {
    "#####",
    "#   ##",
    "# .* #",
    "#  $@#",
    "#  ###",
    "####",
};
静态 常量 字符 *关卡_8_行[] = {
    " ######",
    " #    #",
    "## $*$#",
    "#@$...#",
    "#######",
};
静态 常量 字符 *关卡_9_行[] = {
    "   #####",
    "   #.$ #",
    "   #.$ #",
    "####.# #",
    "#@$ *  #",
    "##   ###",
    " #####",
};
静态 常量 字符 *关卡_10_行[] = {
    "######",
    "#    #",
    "#@ * #",
    "# *.###",
    "## $  #",
    " ##   #",
    "  #####",
};
静态 常量 字符 *关卡_11_行[] = {
    "####",
    "#  ##",
    "# $.##",
    "#  *.###",
    "#  **$ #",
    "###   @#",
    "  ######",
};
静态 常量 字符 **关卡行[关卡数] = {
    关卡_0_行,
    关卡_1_行,
    关卡_2_行,
    关卡_3_行,
    关卡_4_行,
    关卡_5_行,
    关卡_6_行,
    关卡_7_行,
    关卡_8_行,
    关卡_9_行,
    关卡_10_行,
    关卡_11_行,
};
静态 常量 整数 关卡高[关卡数] = {
    7,
    5,
    3,
    5,
    6,
    6,
    6,
    6,
    5,
    7,
    7,
    7,
};
静态 常量 字符 *关卡名[关卡数] = {
    "Classic Star",
    "1 L",
    "1 R",
    "2 L",
    "2 R",
    "3 L",
    "3 R",
    "4 L",
    "4 R",
    "5 L",
    "7 L",
    "7 R",
};
静态 常量 字符 *关卡答案[关卡数] = {
    "ULrRldDLUdrRluurDlllURdddlUrurrrDLuuurDldlluuRlddrrddLruulluurRddlUdrddlLuurD",
    "D",
    "L",
    "llUdrruL",
    "ulldLrurrdrddllllU",
    "RdrrrruruulldLrurrddldllU",
    "rUdrruruulllllldRurrrrrddldllllU",
    "LruLulldRlddrU",
    "RRluurDurrD",
    "RdrrUrruuuLrdLrddlldlluR",
    "urrrdLullddRdRUddrruLdlUluluurD",
    "llULdrrruLdlluUlluurDldRldR",
};

结构 历史 {
    整数 玩家x, 玩家y;
    整数 箱x, 箱y, 箱到x, 箱到y;
    整数 是推箱;
};

静态 整数 墙[最大高][最大宽];
静态 整数 目标格[最大高][最大宽];
静态 整数 箱[最大高][最大宽];
静态 整数 宽, 高, 玩家x, 玩家y, 步数, 推次, 已过关;
静态 整数 当前关;
静态 结构 历史 历史栈[最大历史];
静态 整数 历史顶;

静态 空 检查过关() {
    整数 y, x;
    已过关 = 真;
    对于 (y = 0; y < 高; y++) {
        对于 (x = 0; x < 宽; x++) {
            如果 (箱[y][x] && !目标格[y][x]) 已过关 = 假;
        }
    }
}

静态 空 载入关卡(整数 序号) {
    整数 y, x, 行长;
    常量 字符 **行们;
    如果 (序号 < 0) 序号 = ((序号 % 关卡数) + 关卡数) % 关卡数;
    序号 %= 关卡数;
    当前关 = 序号;
    行们 = 关卡行[序号];
    高 = 关卡高[序号];
    宽 = 0;
    对于 (y = 0; y < 高; y++) {
        行长 = (整数)std::strlen(行们[y]);
        如果 (行长 > 宽) 宽 = 行长;
    }
    std::memset(墙, 0, sizeof(墙));
    std::memset(目标格, 0, sizeof(目标格));
    std::memset(箱, 0, sizeof(箱));
    历史顶 = 步数 = 推次 = 已过关 = 0;
    玩家x = 玩家y = 0;
    对于 (y = 0; y < 高; y++) {
        行长 = (整数)std::strlen(行们[y]);
        对于 (x = 0; x < 宽; x++) {
            字符 ch = (x < 行长) ? 行们[y][x] : ' ';
            如果 (ch == '#') 墙[y][x] = 1;
            否则 如果 (ch == '.') 目标格[y][x] = 1;
            否则 如果 (ch == '$') 箱[y][x] = 1;
            否则 如果 (ch == '*') { 箱[y][x] = 1; 目标格[y][x] = 1; }
            否则 如果 (ch == '@') { 玩家x = x; 玩家y = y; }
            否则 如果 (ch == '+') { 玩家x = x; 玩家y = y; 目标格[y][x] = 1; }
        }
    }
}

静态 整数 尝试移动(整数 dx, 整数 dy) {
    整数 nx, ny, bx, by;
    如果 (已过关) 返回 假;
    nx = 玩家x + dx; ny = 玩家y + dy;
    如果 (nx < 0 || ny < 0 || nx >= 宽 || ny >= 高) 返回 假;
    如果 (墙[ny][nx]) 返回 假;
    如果 (箱[ny][nx]) {
        bx = nx + dx; by = ny + dy;
        如果 (bx < 0 || by < 0 || bx >= 宽 || by >= 高) 返回 假;
        如果 (墙[by][bx] || 箱[by][bx]) 返回 假;
        如果 (历史顶 < 最大历史) {
            历史栈[历史顶] = 历史{玩家x, 玩家y, nx, ny, bx, by, 1};
            历史顶++;
        }
        箱[ny][nx] = 0; 箱[by][bx] = 1;
        玩家x = nx; 玩家y = ny;
        步数++; 推次++;
        检查过关();
        返回 真;
    }
    如果 (历史顶 < 最大历史) {
        历史栈[历史顶] = 历史{玩家x, 玩家y, 0, 0, 0, 0, 0};
        历史顶++;
    }
    玩家x = nx; 玩家y = ny;
    步数++;
    返回 真;
}

静态 整数 撤销一步() {
    如果 (历史顶 == 0) 返回 假;
    历史顶--;
    历史 e = 历史栈[历史顶];
    玩家x = e.玩家x; 玩家y = e.玩家y;
    如果 (e.是推箱) {
        箱[e.箱到y][e.箱到x] = 0;
        箱[e.箱y][e.箱x] = 1;
        如果 (推次 > 0) 推次--;
    }
    如果 (步数 > 0) 步数--;
    已过关 = 假;
    返回 真;
}

静态 空 绘制() {
    整数 y, x;
    对于 (y = 0; y < 高; y++) {
        对于 (x = 0; x < 宽; x++) {
            如果 (玩家x == x && 玩家y == y) std::putchar(目标格[y][x] ? '+' : '@');
            否则 如果 (箱[y][x]) std::putchar(目标格[y][x] ? '*' : '$');
            否则 如果 (墙[y][x]) std::putchar('#');
            否则 如果 (目标格[y][x]) std::putchar('.');
            否则 std::putchar(' ');
        }
        std::putchar('\n');
    }
}

静态 空 套用答案() {
    常量 字符 *s = 关卡答案[当前关];
    载入关卡(当前关);
    对于 (整数 i = 0; s[i]; i++) {
        字符 c = s[i];
        如果 (c == 'u' || c == 'U') 尝试移动(0, -1);
        否则 如果 (c == 'd' || c == 'D') 尝试移动(0, 1);
        否则 如果 (c == 'l' || c == 'L') 尝试移动(-1, 0);
        否则 如果 (c == 'r' || c == 'R') 尝试移动(1, 0);
    }
}

整数 主函数(整数 参数数, 字符 **参数) {
    字符 行[最大行长];
    整数 起始 = 0;
    如果 (参数数 > 1) 起始 = std::atoi(参数[1]) - 1;
    如果 (起始 < 0) 起始 = 0;
    载入关卡(起始);
    输出 << "sokoban_丙正正 — 完整游戏（中文 C++）\n";
    输出 << "共 " << 关卡数 << " 关 · 输入 h 帮助\n";
    当 (真) {
        输出 << "\n";
        绘制();
        输出 << "[" << (当前关 + 1) << "] " << 关卡名[当前关]
             << "  moves=" << 步数 << " pushes=" << 推次
             << (已过关 ? "  WIN!" : "") << "\n> ";
        如果 (!std::fgets(行, sizeof(行), stdin)) 跳出;
        {
            字符 *p = 行 + std::strlen(行);
            当 (p > 行 && (p[-1] == '\n' || p[-1] == '\r' || p[-1] == ' ')) { *--p = 0; }
            p = 行;
            当 (*p == ' ') p++;
            如果 (p != 行) std::memmove(行, p, std::strlen(p) + 1);
        }
        如果 (!行[0]) 继续;
        如果 (std::strcmp(行, "q") == 0 || std::strcmp(行, "quit") == 0) 跳出;
        如果 (std::strcmp(行, "h") == 0 || std::strcmp(行, "?") == 0) {
            输出 << "wasd 移动 | z 撤销 | r 重置 | n/p 上下关 | 数字跳关 | sol! 答案 | q 退出\n";
            继续;
        }
        如果 (std::strcmp(行, "r") == 0) { 载入关卡(当前关); 继续; }
        如果 (std::strcmp(行, "n") == 0 || std::strcmp(行, "]") == 0) { 载入关卡(当前关 + 1); 继续; }
        如果 (std::strcmp(行, "p") == 0 || std::strcmp(行, "[") == 0) { 载入关卡(当前关 - 1); 继续; }
        如果 (std::strcmp(行, "z") == 0) { 如果 (!撤销一步()) 输出 << "无法撤销。\n"; 继续; }
        如果 (std::strcmp(行, "sol!") == 0 || std::strcmp(行, "sol") == 0) {
            套用答案();
            如果 (已过关) 输出 << "Level clear!\n";
            继续;
        }
        如果 (行[0] >= '1' && 行[0] <= '9' && std::strspn(行, "0123456789") == std::strlen(行)) {
            整数 n = std::atoi(行);
            如果 (n >= 1 && n <= 关卡数) 载入关卡(n - 1);
            否则 输出 << "关卡 1.." << 关卡数 << "\n";
            继续;
        }
        {
            整数 合法 = 真;
            对于 (字符 *p = 行; *p; p++) {
                字符 c = (字符)std::tolower((unsigned char)*p);
                如果 (c == ' ' || c == '\t') 继续;
                如果 (c == 'w' || c == 'u') 尝试移动(0, -1);
                否则 如果 (c == 's' || c == 'j') 尝试移动(0, 1);
                否则 如果 (c == 'a' || c == 'l') 尝试移动(-1, 0);
                否则 如果 (c == 'd' || c == 'r') 尝试移动(1, 0);
                否则 { 合法 = 假; 跳出; }
            }
            如果 (!合法) { 输出 << "未知命令。h 帮助\n"; 继续; }
        }
        如果 (已过关) {
            输出 << "Level clear!\n";
            如果 (当前关 + 1 < 关卡数) 输出 << "按 n 下一关\n";
        }
    }
    输出 << "再见！\n";
    返回 0;
}
