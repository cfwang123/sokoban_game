# rustapp1 — Rust Sokoban (teaching)

> [中文版](README.ZH.md)

Pure stdlib: core `game.rs` + terminal main loop. Optionally use `macroquad` for graphics.

```bash
cd rustapp1
cargo run
```

Controls: WASD / hjkl move, z undo, r reset, q quit.
