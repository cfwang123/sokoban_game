# pythonapp1 — Python 推箱子（教学）

纯标准库终端版：核心 `game.py` + 主循环 `main.py`。

```bash
cd pythonapp1
python -X utf8 main.py
```

键位：WASD 移动，z 撤销（回退到上一次推箱），r 重置，q 退出。

对照：`pygameapp1/`（Pygame 图形，仿 `html_app`）。
