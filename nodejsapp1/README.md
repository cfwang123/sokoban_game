# nodejsapp1 — Node.js 推箱子（教学）

纯标准库终端版：`game.js` + `main.js`。需要 Node.js 14+。

```bash
cd nodejsapp1
node main.js
# 或
npm start
```

键位：WASD 移动，z 撤销，r 重置，q 退出。

对照：`html_app/`（浏览器 2D）、`chromeext1/` 等扩展内嵌逻辑。
