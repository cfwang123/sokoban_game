# pygameapp1 — Pygame Sokoban

> [中文版](readme.zh.md)

2D desktop Sokoban with Pygame, modeled on html_app (full levels.json).

`ash
cd pygameapp1
pip install pygame
python -X utf8 main.py
`

Controls: arrow keys / WASD, undo/reset UI as in the Chinese doc.
