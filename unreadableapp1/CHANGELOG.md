# Changelog

## 1.0.0 — 2026-08-12

- 初版可玩 Unreadable 推箱子：`sokoban.unr`（纯 `'`/`"` 游戏逻辑）
- `interpreter.py`：Python 3 解释器（迭代解析 + 求值；宿主不写玩法）
- `main.py`：运行 / `--test` 自检 / `--rebuild` 重新生成
- 键位：WASD 移动、`z` 撤销、`r` 重置、`q` 退出
- 生成器：`scripts/_gen_unreadable_sokoban.py`
