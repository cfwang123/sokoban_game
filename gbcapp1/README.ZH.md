# gbcapp1 — Game Boy Color 推箱子（教学）

> [English](readme.md)


在 `gbapp1` 之上增加**逻辑调色板**（墙/箱/目标/玩家分色）。  
GBDK 可用 `-Wm-yc` 等选项打出 CGB 兼容 ROM。

键位与 GB 相同。模拟器：SameBoy / Emulicious / BGB（CGB 模式）。

本仓库不强制编译 `.gbc`。
