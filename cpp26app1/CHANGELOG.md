# Changelog

## 1.0.0 — 2026-08-19

- 由原 `cppxxapp1`（C++23）重命名并扩写为 `cpp26app1`
- 以 `-std=c++26` 覆盖 C++03～C++26 代表性语法 / 库用法（含 `#embed`、pack indexing、placeholder `_`、`=delete("reason")` 等）
- 迷你关卡经 `level.min.txt` + `#embed` 嵌入
