# wpfapp1 — WPF 推箱子（教学）

C# **WPF** 教学源码（XAML + code-behind），**不要求在本仓库内编译**。

可选本机运行（.NET SDK，Windows）：

```bash
cd wpfapp1
dotnet run
```

键位：WASD / 方向键，Z 撤销，R 重置，Esc/Q 退出。

对照：WinForms [`../winformsapp1`](../winformsapp1) · Avalonia [`../avaloniaapp1`](../avaloniaapp1)
