# electronapp1 — Electron 推箱子桌面 demo（教学）

> [English](readme.md)


用 Electron 包装 Canvas 游戏（逻辑仿 `html_app`）。需要 Node.js。

```bash
cd electronapp1
npm install
npm start
```

键位：WASD / 方向键，Z 撤销，R 重置；点击空地 BFS 寻路。

对照：`html_app/`、`nodejsapp1/`。
